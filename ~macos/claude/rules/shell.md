# Shell

- Target macOS zsh with standard BSD/coreutils tooling.
- Quote paths and variables; assume filenames can contain spaces.
- Don't run destructive commands (`rm -rf`, overwrites) without flagging them first.
