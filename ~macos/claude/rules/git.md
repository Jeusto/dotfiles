# Git

- Never commit or push unless I explicitly ask.
- Never force-push or rewrite history unless I explicitly ask.
- Use Conventional Commit messages: `feat:`, `fix:`, `chore:`, `docs:`, `refactor:`, etc.
- Keep commit subjects short and imperative ("add X", not "added X").
- Don't add co-author trailers or tool attribution unless I ask.
